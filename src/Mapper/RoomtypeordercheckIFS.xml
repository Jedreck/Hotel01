<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Mapper.RoomtypeordercheckIFS">
    <select id="selectNowToFuture" resultType="int">
        select COUNT(*)
        from roomtypeordercheck,roomtype
        where roomtypeordercheck.R_roomtype=#{roomType} and optime between '${startDate}' and '${endDate}' and ordernum&lt;roomnum
    </select>
</mapper>